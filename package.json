{
  "name": "yard-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && echo 'Deploying static files and serverless functions'"
  },
  "dependencies": {
    "firebase": "^12.13.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
